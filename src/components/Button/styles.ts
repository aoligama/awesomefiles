import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    button: {
        height: '10%',
        width: '80%',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
        alignContent: 'center',
        borderRadius: 15,
        margin: 10
    },
    title: {
        fontSize: 14,
        marginLeft: 2,
        fontWeight: '800'
    },
})